/** Import only from here — never internal files (enforced by `no-restricted-imports`). */
export {
  categoryRepository,
  type Category,
  type CategoryListFilter,
} from './infrastructure/categoryRepository';
