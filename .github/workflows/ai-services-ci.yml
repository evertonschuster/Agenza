name: AI Services CI

on:
  pull_request:
    branches: [main]
    paths:
      - 'ai-services/**'
      - '.github/workflows/ai-services-ci.yml'
  push:
    branches: [main]
    paths:
      - 'ai-services/**'
      - '.github/workflows/ai-services-ci.yml'

concurrency:
  group: ai-services-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ai-services-build-and-test:
    name: ai-services-build-and-test
    runs-on: ubuntu-latest
    permissions:
      contents: read
    defaults:
      run:
        working-directory: ai-services/assistant-service
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false

      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: ai-services/assistant-service/pyproject.toml

      - name: Install dependencies
        run: pip install -e ".[dev]"

      - name: Lint
        run: ruff check app tests

      - name: Format check
        run: ruff format --check app tests

      - name: Test (coverage gate configured in pyproject.toml, 80%)
        run: pytest
